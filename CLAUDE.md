# CLAUDE.md

**`AGENTS.md` を読んでください。**内容は同じです（Codex など他のエージェントと同じ
指示で動かすため、実体はそちらに置いています）。

Claude だけができること＝**アーティファクトへの発行**です。手順と注意は `AGENTS.md` の
「できないこと：アーティファクトへの発行」にあります。とくに、**発行の前に公開中の版を
読み出して差分を確かめる**こと（アプリから直接追加された問題を消さないため）。
